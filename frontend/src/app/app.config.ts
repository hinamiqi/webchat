/**
 * GLOBAL CONFIG FILE
 * ==============================================
 * This file contains global application settings.
 *
 * In many cases, those settings duplicates server settings
 * and therefore shouldn't be changed without corresponding
 * server side checks.
*/

export const DEFAULT_MSG_REMOVE_TIME_MINUTES = 1;

export const AUTH_CHECK_INTERVAL_MS = 1000;

export const USER_STATUS_EXPIRATION_MINUTES = 1;
